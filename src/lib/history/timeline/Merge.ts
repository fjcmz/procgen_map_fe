/**
 * Merge — placeholder class.
 * Reserved for future functionality (e.g., peaceful country merging).
 * No generation logic exists for this class.
 */
export interface Merge {
  readonly id: string;
}
